#include "main.h"
#include <stdio.h>

/**
  * add - adds two integers
  *
  * @a: first integer
  * @b: second integer
  *
  * Description:  function that adds two integers and returns the result
  *
  * Return: Always 0.
  */
int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
